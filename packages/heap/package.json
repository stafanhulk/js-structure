{
  "name": "@js-structure/heap",
  "version": "1.0.0",
  "description": "expand heap data structure for javascript",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "test": "jest",
    "clean": "rm -rf dist",
    "prepublish": "pnpm run test"
  },
  "keywords": [
    "heap",
    "min-heap",
    "max-heap",
    "priority-queue",
    "typescript"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "tsup": "^7.2.0"
  }
}
